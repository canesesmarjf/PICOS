b02859b We have incorporated all the source code from the particle_tree development work tested previously. We have included them in the makefile, we have created an instance of the resampling operator and we have created an instance of it with one particle_tree object for each ion species. We have succesfully compiled it. we now need to add a resample switch in the input file, code to determine when to resample and the actual resample operattion. Then we need to test it
